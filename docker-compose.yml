services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.yadikasoreang.web.id}
        VITE_WHATSAPP_NUMBER: ${VITE_WHATSAPP_NUMBER:-6281234567890}
    ports:
      - "8081:80"
    restart: unless-stopped